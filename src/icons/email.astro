---
import { Icon } from 'astro-icon/components';

interface Props {
  height: number;
  width: number;
}

const { height, width } = Astro.props;
---

<Icon name="mi:email" width={width} height={height} />