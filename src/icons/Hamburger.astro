---
import { Icon } from 'astro-icon/components';

interface Props {
    height: number;
    width: number;
  }
  
  const { height, width } = Astro.props;
---
<Icon name="charm:menu-hamburger" width={width} height={height} />

